@AGENTS.md

## Claude Code

Each project lives in its own subfolder with its own CLAUDE.md. Start your
session inside the project folder, not here:

    cd <project>/ && claude

That way the project's STATE.md and BRIEF.md are imported into context at
launch. Starting here gives you this contract but none of the project state.
